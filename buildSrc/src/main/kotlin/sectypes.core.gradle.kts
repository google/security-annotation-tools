/*
 * Includes the various pieces of logic to be shared by most modules. Should set
 * up any IDE configurations and tools that will be required by the build
 * process.
 */

plugins {
    idea
    id("sectypes.internal.spotless")
}
